import React from 'react'

export const CurrentUserContext = React.createContext()

/*export const currentUserData = {
	name: currentUserData.name,
	about: currentUserData.name,
	avatar: currentUserData.name,
}*/
